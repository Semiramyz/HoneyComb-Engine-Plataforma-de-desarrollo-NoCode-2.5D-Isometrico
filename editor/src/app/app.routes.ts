// Tabla de rutas del editor.
//
// Esta vacia a proposito: el editor es una sola pantalla con docks fijos (ver
// app.html), asi que no hay ninguna navegacion que declarar. El router queda
// igual configurado en app.config.ts para que agregar una pantalla mas adelante
// (por ejemplo un selector de proyectos) sea solo sumar una entrada a esta
// lista.

import { Routes } from '@angular/router';

export const routes: Routes = [];
